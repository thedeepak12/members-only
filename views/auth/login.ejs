<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/output.css">
  <title>Login</title>
</head>
<body class="bg-gray-900">
  <div class="bg-gray-900 flex flex-col items-center justify-center h-screen">
    <div class="bg-gray-800 border-[1px] border-gray-700 text-white p-8 rounded-lg">
      <h1 class="text-center text-2xl font-bold mb-6">Log In</h1>
      <form class="flex flex-col gap-4 w-[400px]" action="/login" method="POST">
        <div class="flex flex-col gap-1">
          <label for="username">Username</label>
          <input class="p-2 border rounded" type="text" id="username" name="username" required>
        </div>
        <div class="flex flex-col gap-1">
          <label for="password">Password</label>
          <input class="p-2 border rounded" type="password" id="password" name="password" required>
          <% if (typeof loginError !== 'undefined' && loginError) { %>
            <p class="text-sm text-red-600 mt-1">Invalid username or password.</p>
          <% } %>
        </div>
        <button class="bg-blue-500 text-white p-2 cursor-pointer rounded hover:bg-blue-600 transition-colors mt-2" type="submit">
          Log In
        </button>
      </form>
      <p class="mt-4">
        Don't have an account? 
        <a href="/signup" class="text-blue-500 hover:underline">Sign up</a>
      </p>
    </div>
  </div>
</body>
</html>